#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;

long long int p=1; 
int main() 
{
	int x,n;
	cin>>n>>x;
	int j=1;
	for(int i=1;i<=n;i++)
	{
		p=p*(x-i);
		if (j!=i)
		
	}
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
	cout <<p;   
    return 0;
}

